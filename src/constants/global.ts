
export enum MEDIA {
  DESKTOP = 'desktop',
  TABLET = 'tablet',
  MOBILE = 'mobile'
}

export enum BREAKPOINTS {
  TABLET = 768,
  DESKTOP = 1280
}

export enum ROUTES {
  MAIN = '/',
  ABOUT = '/about',
  AUTH = '/auth',
  COMPONENTS = '/components',
  CONTACTS = '/contacts',
  PRODUCTS = '/products',
  NOT_FOUND = '*'
}
